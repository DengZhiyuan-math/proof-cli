---
phase: 04-memory-and-retrieval
plan: 06
type: execute
wave: 6
depends_on:
  - 01
  - 02
  - 03
  - 04
  - 05
files_modified:
  - src/proof_cli/dsl.py
  - src/proof_cli/elaboration.py
  - src/proof_cli/commands.py
  - tests/test_dsl.py
autonomous: true
requirements:
  - DSL-03
  - CHK-01
must_haves:
  truths:
    - the CLI can express formalization recommendations, verification queues, and revalidation actions
    - theorem application checks can be encoded as stronger machine-checkable workflows
  artifacts:
    - src/proof_cli/dsl.py
    - src/proof_cli/elaboration.py
    - src/proof_cli/commands.py
    - tests/test_dsl.py
  key_links:
    - the DSL should fit the verification workflow order from the phase spec
    - reviewable machine-check status must be exposed in command output
---

<objective>
Extend the proof DSL and elaboration layer for formal-bridge workflows.

Purpose: expose commands for formalization recommendation, verification queueing, machine-check status, acceptance, rejection, staleness, and revalidation.
Output: DSL command handling, elaboration rules, and tests for formal-bridge operations.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-memory-and-retrieval/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Extend DSL command surface</name>
  <files>src/proof_cli/dsl.py</files>
  <read_first>.planning/phases/04-memory-and-retrieval/04-CONTEXT.md</read_first>
  <action>Add commands for formalize recommend/show/edit, verify queue/run/status/result/accept/reject/stale, trace machine-check, and revalidate.</action>
  <verify>pytest tests/test_dsl.py -q</verify>
  <acceptance_criteria>
    - formal-bridge commands parse into explicit command objects
    - machine-check status is represented in the CLI model
  </acceptance_criteria>
  <done>DSL supports formal bridge workflows</done>
</task>

<task type="auto">
  <name>Map elaboration to verification state</name>
  <files>src/proof_cli/elaboration.py, src/proof_cli/commands.py</files>
  <read_first>.planning/phases/04-memory-and-retrieval/04-CONTEXT.md</read_first>
  <action>Translate formalization and verification commands into queued fragments, verification results, review events, and revalidation actions with explicit provenance.</action>
  <verify>pytest tests/test_dsl.py -q</verify>
  <acceptance_criteria>
    - elaboration preserves round-trip provenance
    - accepted and rejected results remain auditable
  </acceptance_criteria>
  <done>Elaboration exposes machine-check state</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] formalization and verification commands produce explicit state transitions
- [ ] machine-check status is visible in CLI output
</verification>

<success_criteria>

- The CLI can drive formal bridge and verification flows
- Elaboration creates explicit verification artifacts

</success_criteria>

<output>
After completion, create `.planning/phases/04-memory-and-retrieval/04-06-SUMMARY.md`
</output>
