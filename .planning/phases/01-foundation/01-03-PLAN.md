---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/proof_cli/theorems.py
  - src/proof_cli/proof_state.py
  - src/proof_cli/services.py
  - tests/test_theorems.py
autonomous: true
requirements:
  - THM-01
  - THM-02
  - THM-03
must_haves:
  truths:
    - theorem-like results are stored as callable contracts rather than notes
    - the system can tell whether a result is callable in the current proof context
  artifacts:
    - src/proof_cli/theorems.py
    - src/proof_cli/proof_state.py
    - tests/test_theorems.py
  key_links:
    - theorem contracts are linked to proof state and dependencies
    - callable results preserve source metadata and trust level
---

<objective>
Make theorem-like results first-class, callable proof contracts.

Purpose: validate the trust-boundary model by storing reusable results with assumptions, exports, dependencies, and trust metadata.
Output: theorem add/show/list/update flows and callability checks against the current proof context.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Implement theorem registry operations</name>
  <files>src/proof_cli/theorems.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement theorem add/show/list/update operations that store structured theorem contracts. Preserve the exact metadata fields from the phase spec, including source_ref, notes, status, trust_level, dependencies, and version. Do not collapse the contract to freeform text.</action>
  <verify>pytest tests/test_theorems.py -q</verify>
  <acceptance_criteria>
    - theorem contracts are listable and inspectable
    - updates preserve version history
  </acceptance_criteria>
  <done>The registry stores structured callable contracts</done>
</task>

<task type="auto">
  <name>Implement callability checks</name>
  <files>src/proof_cli/proof_state.py, src/proof_cli/services.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement the logic that decides whether a theorem contract is callable in the current proof context. Check assumptions, trust level, status, and dependencies before a contract can be applied or imported into the active proof target.</action>
  <verify>pytest tests/test_theorems.py -q</verify>
  <acceptance_criteria>
    - the system can explain why a contract is callable or blocked
    - the check uses current proof context, not just contract presence
  </acceptance_criteria>
  <done>Callable results are gated by proof context</done>
</task>

<task type="auto">
  <name>Support theorem application flow</name>
  <files>src/proof_cli/proof_state.py, src/proof_cli/services.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement theorem apply semantics so calling a theorem checks assumptions instead of re-proving the conclusion. When the call is legal, record the usage in proof state and event history.</action>
  <verify>pytest tests/test_theorems.py -q</verify>
  <acceptance_criteria>
    - theorem application creates a recorded usage event
    - illegal theorem use is rejected with an explicit reason
  </acceptance_criteria>
  <done>Theorem application is operational, not just descriptive</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pytest tests/test_theorems.py -q`
- [ ] theorem add/show/list/update works against persisted storage
- [ ] theorem application rejects illegal calls with a reason
</verification>

<success_criteria>

- Theorem contracts are explicit, versioned, and inspectable
- Context-sensitive callability is enforced
- The system can reuse known results without re-proving them

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
