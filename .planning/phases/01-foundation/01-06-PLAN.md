---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 03
  - 04
  - 05
files_modified:
  - src/proof_cli/checks.py
  - src/proof_cli/snapshot.py
  - src/proof_cli/memory.py
  - tests/test_checks.py
  - tests/test_snapshot.py
autonomous: true
requirements:
  - WS-02
must_haves:
  truths:
    - the system catches the highest-value proof-discipline errors before reuse
    - snapshots preserve enough state to hand off and resume work
  artifacts:
    - src/proof_cli/checks.py
    - src/proof_cli/snapshot.py
    - src/proof_cli/memory.py
    - tests/test_checks.py
    - tests/test_snapshot.py
  key_links:
    - checker output is tied to theorem contracts and proof state
    - snapshots include handoff data and unresolved trust-sensitive calls
---

<objective>
Add the checker stack and the snapshot/memory layer.

Purpose: catch obvious workflow errors early and make interrupted proof work recoverable.
Output: basic proof-discipline checks plus persisted handoff summaries and memory records.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Implement checker stack</name>
  <files>src/proof_cli/checks.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement the required Phase 1 checkers: assumption presence, theorem-call legality, dependency existence, simple circular dependency detection, export-strength mismatch warning, unresolved omission marker, and notation drift warning. Keep checks deterministic and explain failures in terms a researcher can act on.</action>
  <verify>pytest tests/test_checks.py -q</verify>
  <acceptance_criteria>
    - all required checks produce explicit pass/fail/warn output
    - warnings distinguish from hard failures
  </acceptance_criteria>
  <done>Illegal or risky proof reuse is caught early</done>
</task>

<task type="auto">
  <name>Implement snapshot and handoff summaries</name>
  <files>src/proof_cli/snapshot.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Persist snapshot objects that include current theorem, active goals, open obligations, blockers, recent theorem usage, unresolved trust-sensitive calls, and suggested next steps. Make the handoff summary deterministic and easy to inspect after interruption.</action>
  <verify>pytest tests/test_snapshot.py -q</verify>
  <acceptance_criteria>
    - snapshot restoration reproduces the current proof target and open work
    - handoff summaries include unresolved trust-sensitive calls
  </acceptance_criteria>
  <done>Interrupted proof work can be resumed coherently</done>
</task>

<task type="auto">
  <name>Persist proof memory layers</name>
  <files>src/proof_cli/memory.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement working, semantic, episodic, and procedural memory storage for proof projects. Keep memory structured and queryable so it supports retrieval-first behavior instead of becoming a text dump.</action>
  <verify>pytest tests/test_snapshot.py -q</verify>
  <acceptance_criteria>
    - proof memory is split into layered records
    - recent theorem usage is recoverable from storage
  </acceptance_criteria>
  <done>Memory supports retrieval and handoff, not just notes</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pytest tests/test_checks.py -q`
- [ ] `pytest tests/test_snapshot.py -q`
- [ ] checker output includes both warnings and hard failures where appropriate
- [ ] snapshots restore the active proof target and unresolved debts
</verification>

<success_criteria>

- The checker stack catches the required discipline failures
- Snapshots and memory survive interruption
- Handoff data makes proof work recoverable

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
