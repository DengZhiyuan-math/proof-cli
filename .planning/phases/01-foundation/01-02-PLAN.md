---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/proof_cli/cli.py
  - src/proof_cli/commands.py
  - src/proof_cli/rendering.py
  - tests/test_cli.py
autonomous: true
requirements:
  - WS-02
must_haves:
  truths:
    - the CLI can initialize, inspect, and export a proof workspace
    - the CLI exposes readable status for current goals, blockers, and snapshots
  artifacts:
    - src/proof_cli/cli.py
    - src/proof_cli/commands.py
    - src/proof_cli/rendering.py
    - tests/test_cli.py
  key_links:
    - CLI commands read from the same store created by Plan 01
    - status and export output are derived from persisted proof state
---

<objective>
Build the terminal-native command surface for Phase 1.

Purpose: make proof-state operations usable day-to-day without editing files manually.
Output: command parser, status rendering, and auditable export/handoff commands.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Wire CLI entrypoints</name>
  <files>src/proof_cli/cli.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement the `proof` command group and required subcommands: init, status, goal set, goal list, theorem add, theorem show, theorem list, theorem apply, obligation add, obligation list, blocker add, blocker list, snapshot, history, export. Keep the parser minimal and route commands to service functions rather than embedding logic in the CLI layer.</action>
  <verify>pytest tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - All required commands are present
    - Help output lists the Phase 1 command set
  </acceptance_criteria>
  <done>The CLI exposes the Phase 1 workflow surface</done>
</task>

<task type="auto">
  <name>Render readable proof status</name>
  <files>src/proof_cli/rendering.py, src/proof_cli/commands.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement status and export rendering that makes current theorem, open goals, open obligations, blockers, recent results, and unresolved trust-sensitive calls obvious at a glance. Use Rich-friendly formatting and keep output auditable, not decorative.</action>
  <verify>pytest tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - status output includes goals, obligations, blockers, and latest snapshot
    - export output is stable and human-readable
  </acceptance_criteria>
  <done>Status and export are readable enough for real proof work</done>
</task>

<task type="auto">
  <name>Implement handoff summary command</name>
  <files>src/proof_cli/commands.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Add the summary/export path that produces a proof handoff artifact with the current theorem, active goals, blockers, obligations, and next-step suggestions. Keep the output aligned with the persisted snapshot so interrupted work can resume coherently.</action>
  <verify>pytest tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - proof export writes a deterministic handoff summary
    - summary includes unresolved trust-sensitive calls and next steps
  </acceptance_criteria>
  <done>The CLI can export a usable handoff summary</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pytest tests/test_cli.py -q`
- [ ] `proof --help` shows every required subcommand
- [ ] export output includes current theorem, goals, obligations, and blockers
</verification>

<success_criteria>

- The CLI is usable without editing files manually
- The command surface covers the Phase 1 workflow
- Status and export are derived from persisted state, not transient memory

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
