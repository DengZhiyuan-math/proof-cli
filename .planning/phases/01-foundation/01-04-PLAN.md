---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 03
files_modified:
  - src/proof_cli/goals.py
  - src/proof_cli/obligations.py
  - src/proof_cli/blockers.py
  - src/proof_cli/proof_state.py
  - tests/test_proof_state.py
autonomous: true
requirements:
  - WS-02
must_haves:
  truths:
    - open goals, obligations, blockers, failed attempts, and session history are explicit state
    - the current proof target is always recoverable
  artifacts:
    - src/proof_cli/goals.py
    - src/proof_cli/obligations.py
    - src/proof_cli/blockers.py
    - src/proof_cli/proof_state.py
    - tests/test_proof_state.py
  key_links:
    - active theorem and proof obligations are linked
    - blockers and failed routes are tied to concrete proof targets
---

<objective>
Make proof work explicit, resumable, and auditable.

Purpose: ensure the CLI can represent current targets, open goals, obligations, blockers, failed routes, and session history as first-class state.
Output: proof-state engine with goal, obligation, blocker, and failure tracking.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Implement goal and blocker state</name>
  <files>src/proof_cli/goals.py, src/proof_cli/blockers.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement current proof target selection, goal listing, blocker recording, and failed-route notes. Keep the state model explicit so the CLI can resume the same proof target after interruption.</action>
  <verify>pytest tests/test_proof_state.py -q</verify>
  <acceptance_criteria>
    - active theorem/current target is persisted
    - blocker records reference a scope and failure type
  </acceptance_criteria>
  <done>Goals and blockers are recoverable from state</done>
</task>

<task type="auto">
  <name>Implement obligation queue</name>
  <files>src/proof_cli/obligations.py, src/proof_cli/proof_state.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Add an obligation queue that tracks open obligations, required-for links, priority, blocking reason, and dependencies. Preserve source step links so unresolved proof debts can be traced back to the originating action.</action>
  <verify>pytest tests/test_proof_state.py -q</verify>
  <acceptance_criteria>
    - obligations can be added, listed, and linked to source steps
    - open obligations survive restart
  </acceptance_criteria>
  <done>Proof obligations are explicit and trackable</done>
</task>

<task type="auto">
  <name>Persist session history and snapshots</name>
  <files>src/proof_cli/proof_state.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Record session history entries and latest snapshot pointers so interruption and recovery are coherent. Include the most recent theorem usage, blockers, obligations, and next-step suggestions.</action>
  <verify>pytest tests/test_proof_state.py -q</verify>
  <acceptance_criteria>
    - session history is queryable
    - latest snapshot can be restored as the current state
  </acceptance_criteria>
  <done>Proof sessions are resumable and auditable</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pytest tests/test_proof_state.py -q`
- [ ] blockers and obligations survive interruption
- [ ] session history links to current proof target
</verification>

<success_criteria>

- The proof state engine exposes open work explicitly
- Failed proof routes are recoverable
- The active target and obligations survive a new session

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
