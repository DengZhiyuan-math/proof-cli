---
phase: 02-registry
plan: 07
type: execute
wave: 7
depends_on:
  - 03
  - 06
files_modified:
  - src/proof_cli/checks.py
  - src/proof_cli/review.py
  - tests/test_checks.py
  - tests/test_review.py
autonomous: true
requirements:
  - CHK-01
  - CHK-02
  - CHK-03
  - CHK-04
must_haves:
  truths:
    - imported theorem use is checked against assumptions, exports, and provenance
    - omission and notation drift are surfaced as actionable checker output
  artifacts:
    - src/proof_cli/checks.py
    - src/proof_cli/review.py
    - tests/test_checks.py
    - tests/test_review.py
  key_links:
    - checker output should warn rather than silently accept weak grounding
    - review flow remains the human gate for trust-sensitive changes
---

<objective>
Expand the checker stack for literature-aware proof work.

Purpose: detect assumption mismatches, export overstretch, circular dependencies, unresolved omissions, and notation drift before imported results are reused.
Output: stronger checker rules, review hooks, and validation tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-registry/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Strengthen proof discipline checks</name>
  <files>src/proof_cli/checks.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Check theorem applicability, assumption presence, export-strength mismatch, dependency existence, circular dependency risk, unresolved omissions, and notation drift.</action>
  <verify>pytest tests/test_checks.py -q</verify>
  <acceptance_criteria>
    - imported theorem use is rejected or warned when assumptions do not match
    - export overstretch and omission gaps are surfaced clearly
  </acceptance_criteria>
  <done>Checker discipline is strong enough for reviewable reuse</done>
</task>

<task type="auto">
  <name>Wire review hooks to checker output</name>
  <files>src/proof_cli/review.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Route trust-sensitive change requests through explicit confirmation and audit logging, using checker warnings as the review context.</action>
  <verify>pytest tests/test_review.py -q</verify>
  <acceptance_criteria>
    - unresolved external-use cases remain visible to the reviewer
    - trust-sensitive actions do not bypass the review gate
  </acceptance_criteria>
  <done>Checker output is connected to human review</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] assumption mismatch, export overstretch, and omission warnings are covered
- [ ] review gates remain explicit for trust-sensitive actions
</verification>

<success_criteria>

- Imported theorem use is checker-guarded
- Weak grounding is surfaced before reuse
- Review uses checker output as evidence

</success_criteria>

<output>
After completion, create `.planning/phases/02-registry/02-07-SUMMARY.md`
</output>
