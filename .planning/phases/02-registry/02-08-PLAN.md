---
phase: 02-registry
plan: 08
type: execute
wave: 8
depends_on:
  - 01
  - 02
  - 03
  - 04
  - 05
  - 06
  - 07
files_modified:
  - src/proof_cli/cli.py
  - src/proof_cli/commands.py
  - src/proof_cli/export.py
  - tests/test_cli.py
  - tests/test_export.py
autonomous: true
requirements:
  - REV-01
  - REV-02
must_haves:
  truths:
    - trust-sensitive imported results require explicit approval, downgrade, or rejection
    - the phase must be validated on a real proof workflow, not only toy examples
  artifacts:
    - src/proof_cli/cli.py
    - src/proof_cli/commands.py
    - src/proof_cli/export.py
    - tests/test_cli.py
    - tests/test_export.py
  key_links:
    - the CLI should expose search, import, grounding, memory, and provenance workflows
    - export should describe proved, assumed, open, and externally grounded state
---

<objective>
Complete the Phase 2 workflow and validate it on a real proof project.

Purpose: surface the retrieval-first CLI surface, keep human review explicit, and prove the workflow on a nontrivial research proof project.
Output: completed CLI paths, auditable export, and validation evidence for the real project target.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-registry/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Expose Phase 2 CLI workflows</name>
  <files>src/proof_cli/cli.py, src/proof_cli/commands.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Expose retrieval, reference import/review, theorem grounding, memory, and provenance commands in the terminal interface with readable status output.</action>
  <verify>pytest tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - users can reach the Phase 2 workflows from the CLI
    - command output is readable enough for session-to-session use
  </acceptance_criteria>
  <done>CLI surface matches the retrieval-first workflow</done>
</task>

<task type="auto">
  <name>Complete export and validation harness</name>
  <files>src/proof_cli/export.py, tests/test_export.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Create an auditable export that summarizes proved, assumed, open, imported, and grounded state, then run the real proof-project validation harness with at least one imported standard result, one imported research-paper result, one blocker, and one lingering obligation.</action>
  <verify>pytest tests/test_cli.py tests/test_export.py -q</verify>
  <acceptance_criteria>
    - export reflects project state and external grounding
    - the validation project survives multiple sessions with useful context preserved
  </acceptance_criteria>
  <done>Phase 2 is validated on a real workflow</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] the CLI exposes retrieval, import, grounding, memory, and provenance workflows
- [ ] the export and validation harness show the phase value on a real project
</verification>

<success_criteria>

- Trust-sensitive imported results remain reviewable
- Export output is auditable and session-stable
- Phase 2 is demonstrated on a nontrivial proof workflow

</success_criteria>

<output>
After completion, create `.planning/phases/02-registry/02-08-SUMMARY.md`
</output>
