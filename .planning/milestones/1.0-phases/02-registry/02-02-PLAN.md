---
phase: 02-registry
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/proof_cli/references.py
  - src/proof_cli/storage.py
  - tests/test_references.py
autonomous: true
requirements:
  - RET-02
  - REV-01
must_haves:
  truths:
    - external references become structured project objects before they become callable
    - imported references preserve bibliographic provenance and review state
  artifacts:
    - src/proof_cli/references.py
    - src/proof_cli/storage.py
    - tests/test_references.py
  key_links:
    - import is a reviewable state transition, not a text paste
    - rejection and deferral remain explicit project records
---

<objective>
Build the reference import pipeline for Phase 2.

Purpose: turn selected external references into structured records with review status, provenance, and callable/non-callable state.
Output: import workflow, review records, and source-linked reference storage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-registry/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Define reference schema</name>
  <files>src/proof_cli/references.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Create ReferenceRecord and imported review models with bibliographic metadata, review status, trust level, and provenance fields.</action>
  <verify>pytest tests/test_references.py -q</verify>
  <acceptance_criteria>
    - reference records persist title, authors, year, source type, origin, identifier, url, review status, and trust level
    - provenance is retained through validation and serialization
  </acceptance_criteria>
  <done>References are modeled as durable project objects</done>
</task>

<task type="auto">
  <name>Implement import-review flow</name>
  <files>src/proof_cli/storage.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Persist candidate references, import approvals, rejections, and deferrals as auditable project events instead of overwriting them silently.</action>
  <verify>pytest tests/test_references.py -q</verify>
  <acceptance_criteria>
    - import review creates a durable record
    - rejected imports remain visible for later inspection
  </acceptance_criteria>
  <done>Reference import is auditable and reversible</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] imported references preserve bibliographic provenance
- [ ] candidate, approved, rejected, and deferred states are all explicit
</verification>

<success_criteria>

- External references are stored as structured project objects
- Import review is explicit and auditable
- Rejected or deferred references do not disappear

</success_criteria>

<output>
After completion, create `.planning/phases/02-registry/02-02-SUMMARY.md`
</output>
