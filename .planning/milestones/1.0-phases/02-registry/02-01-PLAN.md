---
phase: 02-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/proof_cli/retrieval.py
  - src/proof_cli/services.py
  - tests/test_retrieval.py
autonomous: true
requirements:
  - RET-01
  - RET-02
must_haves:
  truths:
    - retrieval begins with current project state before any external search
    - candidate references are ranked and separated from callable theorem contracts
  artifacts:
    - src/proof_cli/retrieval.py
    - src/proof_cli/services.py
    - tests/test_retrieval.py
  key_links:
    - project-local search should be the default retrieval path
    - search results stay candidate until import or review changes their state
---

<objective>
Create the retrieval skeleton for Phase 2.

Purpose: define the retrieval hierarchy and normalize candidate outputs from project-local results, imported references, and external sources.
Output: retrieval orchestrator, ranked candidate model, and tests for retrieval ordering.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-registry/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Define retrieval primitives</name>
  <files>src/proof_cli/retrieval.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Model retrieval sources, ranked candidate results, and a retrieval policy that searches project-local results before imported references and external sources.</action>
  <verify>pytest tests/test_retrieval.py -q</verify>
  <acceptance_criteria>
    - retrieval results include source category and ranking metadata
    - project-local search is evaluated before external search
  </acceptance_criteria>
  <done>Retrieval ordering is explicit and testable</done>
</task>

<task type="auto">
  <name>Wire retrieval into services</name>
  <files>src/proof_cli/services.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Expose retrieval through the application service layer so commands can request the current proof context and return candidate sources in a single structured response.</action>
  <verify>pytest tests/test_retrieval.py -q</verify>
  <acceptance_criteria>
    - services can request retrieval from the current project context
    - the result payload is reusable by later import and grounding commands
  </acceptance_criteria>
  <done>Retrieval is available to the proof workflow</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] retrieval returns ranked candidates from project-local and external sources
- [ ] project-local results are searched before external sources
</verification>

<success_criteria>

- Retrieval hierarchy is explicit
- Candidate reference output is structured and ranked
- The application layer can consume retrieval results

</success_criteria>

<output>
After completion, create `.planning/phases/02-registry/02-01-SUMMARY.md`
</output>
