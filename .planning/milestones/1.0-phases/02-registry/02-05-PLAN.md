---
phase: 02-registry
plan: 05
type: execute
wave: 5
depends_on:
  - 04
files_modified:
  - src/proof_cli/memory.py
  - src/proof_cli/snapshot.py
  - tests/test_snapshot.py
autonomous: true
requirements:
  - MEM-01
must_haves:
  truths:
    - proof memory is typed into working, semantic, episodic, and procedural layers
    - snapshots capture more than flat text summaries
  artifacts:
    - src/proof_cli/memory.py
    - src/proof_cli/snapshot.py
    - tests/test_snapshot.py
  key_links:
    - memory updates are integrated into proof events
    - handoff artifacts are recoverable after interruption
---

<objective>
Build the richer memory architecture for Phase 2.

Purpose: persist useful research memory at multiple levels and connect it to session handoff and recovery.
Output: typed memory artifacts, snapshot integration, and recovery tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-registry/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Define memory artifact types</name>
  <files>src/proof_cli/memory.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Create working, semantic, episodic, and procedural memory artifacts with project-scoped retrieval and importance metadata.</action>
  <verify>pytest tests/test_snapshot.py -q</verify>
  <acceptance_criteria>
    - memory artifacts carry type, scope, content, and linked proof state
    - retrieval can separate stable facts from failed routes and tactics
  </acceptance_criteria>
  <done>Memory is structured instead of free-form</done>
</task>

<task type="auto">
  <name>Integrate memory with snapshots</name>
  <files>src/proof_cli/snapshot.py</files>
  <read_first>.planning/phases/02-registry/02-CONTEXT.md</read_first>
  <action>Store handoff snapshots that include memory-relevant state, recent attempts, and unresolved proof debts so interrupted work can resume coherently.</action>
  <verify>pytest tests/test_snapshot.py -q</verify>
  <acceptance_criteria>
    - snapshots restore useful working context
    - recent failures and blockers survive session interruption
  </acceptance_criteria>
  <done>Proof work can be resumed with meaningful context</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] memory artifacts are typed and retrievable
- [ ] snapshots preserve more than a flat summary
</verification>

<success_criteria>

- Memory layers are explicit
- Snapshots support handoff and recovery
- Interrupted proof work can be resumed coherently

</success_criteria>

<output>
After completion, create `.planning/phases/02-registry/02-05-SUMMARY.md`
</output>
