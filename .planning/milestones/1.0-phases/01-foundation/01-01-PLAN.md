---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/proof_cli/domain.py
  - src/proof_cli/storage.py
  - src/proof_cli/db.py
  - tests/test_storage.py
autonomous: true
requirements:
  - WS-01
must_haves:
  truths:
    - proof projects can be initialized and reopened without losing state
    - persisted state includes event history, versioned contracts, and snapshots
  artifacts:
    - src/proof_cli/domain.py
    - src/proof_cli/storage.py
    - src/proof_cli/db.py
    - tests/test_storage.py
  key_links:
    - CLI initialization reads and writes the same project store used by later commands
    - project state can be reloaded after a process restart
---

<objective>
Create the persistent project core and local storage foundation for Phase 1.

Purpose: provide the durable data model that all proof-state, registry, and validation behavior depends on.
Output: schema definitions, project creation/loading, event log persistence, and contract versioning support.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Define core proof objects</name>
  <files>src/proof_cli/domain.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Define typed models for TheoremContract, ProofObligation, BlockerRecord, ProjectSnapshot, and EventRecord. Include the exact fields from the phase spec, use explicit enums for status and trust level, and keep the models serialization-friendly for SQLite and JSON/YAML export.</action>
  <verify>pytest tests/test_storage.py -q</verify>
  <acceptance_criteria>
    - The domain module exports all five core models
    - TheoremContract includes id, kind, name, statement, assumptions, exports, status, trust_level, dependencies, source_ref, notes, version, and timestamps
  </acceptance_criteria>
  <done>Core models exist and round-trip cleanly through validation</done>
</task>

<task type="auto">
  <name>Build local store</name>
  <files>src/proof_cli/storage.py, src/proof_cli/db.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement SQLite persistence for project metadata, event log, versioned theorem contracts, obligations, blockers, and snapshots. Ensure project initialization creates the store, loading restores state, and writes append auditable events instead of mutating state silently.</action>
  <verify>pytest tests/test_storage.py -q</verify>
  <acceptance_criteria>
    - A new project store is created on init
    - The same store can be reopened in a new process
    - Event log entries are appended for state-changing operations
  </acceptance_criteria>
  <done>Storage survives session loss and records auditable history</done>
</task>

<task type="auto">
  <name>Implement versioning strategy</name>
  <files>src/proof_cli/storage.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Add theorem contract version support so replacements and superseding events preserve prior versions instead of overwriting them. Keep the active contract pointer explicit and ensure version bumps are reflected in the event log.</action>
  <verify>pytest tests/test_storage.py -q</verify>
  <acceptance_criteria>
    - Contract history is retained
    - Superseding a contract does not destroy earlier versions
  </acceptance_criteria>
  <done>Contract versioning is explicit and auditable</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pytest tests/test_storage.py -q`
- [ ] project init/open cycle works without manual file edits
- [ ] event log contains a record for each state-changing write
</verification>

<success_criteria>

- Persistent store exists and can be reopened
- Core proof objects are validated and serializable
- Version history is preserved for theorem contracts
- No silent state mutation occurs outside the event log

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
