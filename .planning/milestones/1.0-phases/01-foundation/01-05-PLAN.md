---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 03
  - 04
files_modified:
  - src/proof_cli/dsl.py
  - src/proof_cli/elaboration.py
  - tests/test_dsl.py
autonomous: true
requirements:
  - WS-02
must_haves:
  truths:
    - proof-script actions map to explicit state transitions
    - omitted reasoning generates obligations instead of disappearing
  artifacts:
    - src/proof_cli/dsl.py
    - src/proof_cli/elaboration.py
    - tests/test_dsl.py
  key_links:
    - DSL parsing feeds the proof-state engine
    - elaboration creates obligations that the checker stack can inspect
---

<objective>
Make proof-script commands operational.

Purpose: ensure the minimal DSL is not just display formatting and that every command creates an explicit state effect.
Output: parser, elaborator, and state-transition logic for the Phase 1 proof language.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Parse the Phase 1 DSL</name>
  <files>src/proof_cli/dsl.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Implement parsing for the exact Phase 1 command set: import T, use T, apply T, goal G, assume H, assert C, defer C, split, suffices Q, close. Preserve enough structure to support later elaboration and checker logic.</action>
  <verify>pytest tests/test_dsl.py -q</verify>
  <acceptance_criteria>
    - every command in the phase spec is recognized
    - parsing returns structured command objects
  </acceptance_criteria>
  <done>DSL commands parse into structured operations</done>
</task>

<task type="auto">
  <name>Elaborate commands into state transitions</name>
  <files>src/proof_cli/elaboration.py, src/proof_cli/proof_state.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Map each DSL command to an explicit proof-state mutation. Distinguish admissible actions from justificatory actions, and generate obligations whenever justification is incomplete or compressed reasoning is used.</action>
  <verify>pytest tests/test_dsl.py -q</verify>
  <acceptance_criteria>
    - each DSL command changes explicit state
    - insufficient justification creates obligations
  </acceptance_criteria>
  <done>DSL actions have real proof-state consequences</done>
</task>

<task type="auto">
  <name>Block vague proof language</name>
  <files>src/proof_cli/elaboration.py</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>Refuse or downgrade vague proof language that would otherwise hide proof debt. Ensure that silent acceptance of compressed steps does not occur unless the system can surface the omitted obligations explicitly.</action>
  <verify>pytest tests/test_dsl.py -q</verify>
  <acceptance_criteria>
    - vague shorthand produces visible obligations or warnings
    - the elaborator never hides unresolved proof debt
  </acceptance_criteria>
  <done>The DSL is operational and discipline-preserving</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pytest tests/test_dsl.py -q`
- [ ] each DSL command maps to an explicit state transition
- [ ] omitted steps produce obligations rather than silent acceptance
</verification>

<success_criteria>

- The DSL is small but operational
- The elaborator creates visible proof obligations when needed
- The command language remains usable for mathematicians

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
